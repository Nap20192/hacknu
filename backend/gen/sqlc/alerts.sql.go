// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: alerts.sql

package sqlc

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const acknowledgeAlert = `-- name: AcknowledgeAlert :one
UPDATE alerts
SET
    acknowledged = TRUE
WHERE
    id = $1
RETURNING
    id,
    locomotive_id,
    triggered_at,
    resolved_at,
    severity,
    code,
    metric_name,
    metric_value,
    threshold,
    message,
    recommendation,
    acknowledged
`

func (q *Queries) AcknowledgeAlert(ctx context.Context, id int64) (Alert, error) {
	row := q.db.QueryRow(ctx, acknowledgeAlert, id)
	var i Alert
	err := row.Scan(
		&i.ID,
		&i.LocomotiveID,
		&i.TriggeredAt,
		&i.ResolvedAt,
		&i.Severity,
		&i.Code,
		&i.MetricName,
		&i.MetricValue,
		&i.Threshold,
		&i.Message,
		&i.Recommendation,
		&i.Acknowledged,
	)
	return i, err
}

const insertAlert = `-- name: InsertAlert :one

INSERT INTO
    alerts (
        locomotive_id,
        severity,
        code,
        metric_name,
        metric_value,
        threshold,
        message,
        recommendation
    )
VALUES (
        $1,
        $2,
        $3,
        $4,
        $5,
        $6,
        $7,
        $8
    )
RETURNING
    id,
    locomotive_id,
    triggered_at,
    resolved_at,
    severity,
    code,
    metric_name,
    metric_value,
    threshold,
    message,
    recommendation,
    acknowledged
`

type InsertAlertParams struct {
	LocomotiveID   uuid.UUID `json:"locomotive_id"`
	Severity       string    `json:"severity"`
	Code           string    `json:"code"`
	MetricName     *string   `json:"metric_name"`
	MetricValue    *float32  `json:"metric_value"`
	Threshold      *float32  `json:"threshold"`
	Message        string    `json:"message"`
	Recommendation string    `json:"recommendation"`
}

// ALERTS
func (q *Queries) InsertAlert(ctx context.Context, arg InsertAlertParams) (Alert, error) {
	row := q.db.QueryRow(ctx, insertAlert,
		arg.LocomotiveID,
		arg.Severity,
		arg.Code,
		arg.MetricName,
		arg.MetricValue,
		arg.Threshold,
		arg.Message,
		arg.Recommendation,
	)
	var i Alert
	err := row.Scan(
		&i.ID,
		&i.LocomotiveID,
		&i.TriggeredAt,
		&i.ResolvedAt,
		&i.Severity,
		&i.Code,
		&i.MetricName,
		&i.MetricValue,
		&i.Threshold,
		&i.Message,
		&i.Recommendation,
		&i.Acknowledged,
	)
	return i, err
}

const listActiveAlertsByLocomotive = `-- name: ListActiveAlertsByLocomotive :many
SELECT
    id,
    locomotive_id,
    triggered_at,
    resolved_at,
    severity,
    code,
    metric_name,
    metric_value,
    threshold,
    message,
    recommendation,
    acknowledged
FROM alerts
WHERE
    locomotive_id = $1
    AND resolved_at IS NULL
ORDER BY triggered_at DESC
`

func (q *Queries) ListActiveAlertsByLocomotive(ctx context.Context, locomotiveID uuid.UUID) ([]Alert, error) {
	rows, err := q.db.Query(ctx, listActiveAlertsByLocomotive, locomotiveID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Alert{}
	for rows.Next() {
		var i Alert
		if err := rows.Scan(
			&i.ID,
			&i.LocomotiveID,
			&i.TriggeredAt,
			&i.ResolvedAt,
			&i.Severity,
			&i.Code,
			&i.MetricName,
			&i.MetricValue,
			&i.Threshold,
			&i.Message,
			&i.Recommendation,
			&i.Acknowledged,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAlertsByLocomotiveRange = `-- name: ListAlertsByLocomotiveRange :many
SELECT
    id,
    locomotive_id,
    triggered_at,
    resolved_at,
    severity,
    code,
    metric_name,
    metric_value,
    threshold,
    message,
    recommendation,
    acknowledged
FROM alerts
WHERE
    locomotive_id = $1
    AND triggered_at >= $2
    AND triggered_at <= $3
ORDER BY triggered_at DESC
`

type ListAlertsByLocomotiveRangeParams struct {
	LocomotiveID  uuid.UUID `json:"locomotive_id"`
	TriggeredAt   time.Time `json:"triggered_at"`
	TriggeredAt_2 time.Time `json:"triggered_at_2"`
}

func (q *Queries) ListAlertsByLocomotiveRange(ctx context.Context, arg ListAlertsByLocomotiveRangeParams) ([]Alert, error) {
	rows, err := q.db.Query(ctx, listAlertsByLocomotiveRange, arg.LocomotiveID, arg.TriggeredAt, arg.TriggeredAt_2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Alert{}
	for rows.Next() {
		var i Alert
		if err := rows.Scan(
			&i.ID,
			&i.LocomotiveID,
			&i.TriggeredAt,
			&i.ResolvedAt,
			&i.Severity,
			&i.Code,
			&i.MetricName,
			&i.MetricValue,
			&i.Threshold,
			&i.Message,
			&i.Recommendation,
			&i.Acknowledged,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const resolveAlertNow = `-- name: ResolveAlertNow :one
UPDATE alerts
SET
    resolved_at = NOW()
WHERE
    id = $1
RETURNING
    id,
    locomotive_id,
    triggered_at,
    resolved_at,
    severity,
    code,
    metric_name,
    metric_value,
    threshold,
    message,
    recommendation,
    acknowledged
`

func (q *Queries) ResolveAlertNow(ctx context.Context, id int64) (Alert, error) {
	row := q.db.QueryRow(ctx, resolveAlertNow, id)
	var i Alert
	err := row.Scan(
		&i.ID,
		&i.LocomotiveID,
		&i.TriggeredAt,
		&i.ResolvedAt,
		&i.Severity,
		&i.Code,
		&i.MetricName,
		&i.MetricValue,
		&i.Threshold,
		&i.Message,
		&i.Recommendation,
		&i.Acknowledged,
	)
	return i, err
}
