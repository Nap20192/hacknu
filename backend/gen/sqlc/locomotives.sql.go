// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: locomotives.sql

package sqlc

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const getLocomotive = `-- name: GetLocomotive :one
SELECT
    id,
    display_name,
    loco_type,
    registered_at,
    last_seen_at,
    active
FROM locomotives
WHERE
    id = $1
`

func (q *Queries) GetLocomotive(ctx context.Context, id uuid.UUID) (Locomotive, error) {
	row := q.db.QueryRow(ctx, getLocomotive, id)
	var i Locomotive
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.LocoType,
		&i.RegisteredAt,
		&i.LastSeenAt,
		&i.Active,
	)
	return i, err
}

const listActiveLocomotives = `-- name: ListActiveLocomotives :many
SELECT
    id,
    display_name,
    loco_type,
    registered_at,
    last_seen_at,
    active
FROM locomotives
WHERE
    active = TRUE
ORDER BY id
`

func (q *Queries) ListActiveLocomotives(ctx context.Context) ([]Locomotive, error) {
	rows, err := q.db.Query(ctx, listActiveLocomotives)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Locomotive{}
	for rows.Next() {
		var i Locomotive
		if err := rows.Scan(
			&i.ID,
			&i.DisplayName,
			&i.LocoType,
			&i.RegisteredAt,
			&i.LastSeenAt,
			&i.Active,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listLocomotives = `-- name: ListLocomotives :many

SELECT
    id,
    display_name,
    loco_type,
    registered_at,
    last_seen_at,
    active
FROM locomotives
ORDER BY id
`

// LOCOMOTIVES
func (q *Queries) ListLocomotives(ctx context.Context) ([]Locomotive, error) {
	rows, err := q.db.Query(ctx, listLocomotives)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Locomotive{}
	for rows.Next() {
		var i Locomotive
		if err := rows.Scan(
			&i.ID,
			&i.DisplayName,
			&i.LocoType,
			&i.RegisteredAt,
			&i.LastSeenAt,
			&i.Active,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setLocomotiveActive = `-- name: SetLocomotiveActive :one
UPDATE locomotives
SET
    active = $2
WHERE
    id = $1
RETURNING
    id,
    display_name,
    loco_type,
    registered_at,
    last_seen_at,
    active
`

type SetLocomotiveActiveParams struct {
	ID     uuid.UUID `json:"id"`
	Active bool      `json:"active"`
}

func (q *Queries) SetLocomotiveActive(ctx context.Context, arg SetLocomotiveActiveParams) (Locomotive, error) {
	row := q.db.QueryRow(ctx, setLocomotiveActive, arg.ID, arg.Active)
	var i Locomotive
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.LocoType,
		&i.RegisteredAt,
		&i.LastSeenAt,
		&i.Active,
	)
	return i, err
}

const updateLocomotiveLastSeen = `-- name: UpdateLocomotiveLastSeen :one
UPDATE locomotives
SET
    last_seen_at = $2
WHERE
    id = $1
RETURNING
    id,
    display_name,
    loco_type,
    registered_at,
    last_seen_at,
    active
`

type UpdateLocomotiveLastSeenParams struct {
	ID         uuid.UUID  `json:"id"`
	LastSeenAt *time.Time `json:"last_seen_at"`
}

func (q *Queries) UpdateLocomotiveLastSeen(ctx context.Context, arg UpdateLocomotiveLastSeenParams) (Locomotive, error) {
	row := q.db.QueryRow(ctx, updateLocomotiveLastSeen, arg.ID, arg.LastSeenAt)
	var i Locomotive
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.LocoType,
		&i.RegisteredAt,
		&i.LastSeenAt,
		&i.Active,
	)
	return i, err
}

const upsertLocomotive = `-- name: UpsertLocomotive :one
INSERT INTO
    locomotives (
        id,
        display_name,
        loco_type,
        last_seen_at,
        active
    )
VALUES ($1, $2, $3, $4, $5)
ON CONFLICT (id) DO
UPDATE
SET
    display_name = EXCLUDED.display_name,
    loco_type = EXCLUDED.loco_type,
    last_seen_at = EXCLUDED.last_seen_at,
    active = EXCLUDED.active
RETURNING
    id,
    display_name,
    loco_type,
    registered_at,
    last_seen_at,
    active
`

type UpsertLocomotiveParams struct {
	ID          uuid.UUID  `json:"id"`
	DisplayName string     `json:"display_name"`
	LocoType    string     `json:"loco_type"`
	LastSeenAt  *time.Time `json:"last_seen_at"`
	Active      bool       `json:"active"`
}

func (q *Queries) UpsertLocomotive(ctx context.Context, arg UpsertLocomotiveParams) (Locomotive, error) {
	row := q.db.QueryRow(ctx, upsertLocomotive,
		arg.ID,
		arg.DisplayName,
		arg.LocoType,
		arg.LastSeenAt,
		arg.Active,
	)
	var i Locomotive
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.LocoType,
		&i.RegisteredAt,
		&i.LastSeenAt,
		&i.Active,
	)
	return i, err
}
